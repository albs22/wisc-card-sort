CardSort.config = {
	//stores survey code
      code: '00',
	 //stores if the take page has been exited to be determined if needs to be refreshed
	  isReload: false,
      //set to false after index function is laoded to display network status
      intialLoad: true,
	  //path to webservice
	  //webserviceURL: "http://alexbehlen.com/webservice.php"
	
}

